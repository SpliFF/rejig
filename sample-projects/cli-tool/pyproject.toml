[project]
name = "cli-tool"
version = "1.0.0"
description = "Sample CLI tool"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0.0",
]

[project.scripts]
mycli = "cli:cli"

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]
